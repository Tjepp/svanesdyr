import styled from 'styled-components';

const StyledExternal = styled.a`
  color: #008080;
  font-family: inherit;
  font-weight: bold;
  text-decoration: none;
  cursor: pointer;
`;

export default StyledExternal;
